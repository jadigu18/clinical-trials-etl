services:
  app:
    build: .
    environment:
      - DB_HOST=db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: clinical
    ports:
      - "5432:5432"